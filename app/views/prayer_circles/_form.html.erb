<%= form_with(model: prayer_circle) do |form| %>
  <% if prayer_circle.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(prayer_circle.errors.count, "error") %> prohibited this prayer_circle from being saved:</h2>

      <ul>
        <% prayer_circle.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.textarea :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
